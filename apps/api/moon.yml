type: "application"
language: "typescript"

fileGroups:
  app:
    - "modules/**/*"
    - "index.ts"

tasks:
  dev:
    command: "dotenvx run -- bun run --hot index.ts"
  start:
    command: "bun --env-file=.env.decrypted index.ts"
