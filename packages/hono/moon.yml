type: "library"
language: "typescript"

tasks:
  build:
    options:
      mergeArgs: "replace"
    args: "tsup --entry src/index.ts --entry src/**/*.ts --clean --dts --format esm --tsconfig tsconfig.build.json"
