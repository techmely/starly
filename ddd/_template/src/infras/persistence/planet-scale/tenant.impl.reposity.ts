// import { getDBClient } from "./planet-scale.config";

// export class TPlanetScaleRepository
//   extends MySQLRepositoryBase<TEntity, TModel, { Ts: TModel }>
//   implements ITRepository
// {
//   protected tableName = "Ts";
//   protected db = getDBClient();

//   constructor(mapper: TMapper, emitter: Emittery<EmitDomainEvents>) {
//     super(mapper, emitter, consola);
//   }
// }
