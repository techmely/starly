export const initGlobalActions = () => {
  async function refreshUser() {
    // try {
    //   const idToken = cookieServices.getToken() || "";
    //   if (!idToken) return;
    //   const accountInfo = await googleAccountLookUp(firebaseConfig, {
    //     idToken,
    //   });
    //   if (accountInfo) {
    //     // setAuth(produce)
    //   }
    // } catch (error) {
    //   console.error(error);
    // }
  }
};
